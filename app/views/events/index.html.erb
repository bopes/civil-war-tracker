<%= provide(:title, 'Events') %>

<h1>Events</h1>

<div class='row list-narrow-wrapper'>
  <div class='list-narrow-header'>
    <h3></h3>
  </div>
  <div class='list-narrow'>
    <%= render partial: 'layouts/list', locals: { list: @events } %>
  </div>
</div>

<div class='row center button-actions'>
  <%= link_to "Add Event", new_event_path, class: 'btn btn-primary' %>
</div>